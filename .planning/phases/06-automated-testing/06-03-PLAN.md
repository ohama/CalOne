---
phase: 06-automated-testing
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/history.test.js
autonomous: true

must_haves:
  truths:
    - "localStorage persistence is tested with real browser API"
    - "History max entries limit (50) is enforced in tests"
    - "Quota exceeded handling is verified"
  artifacts:
    - path: "tests/history.test.js"
      provides: "Browser-mode tests for localStorage history"
      min_lines: 80
      contains: "@vitest-environment browser"
  key_links:
    - from: "tests/history.test.js"
      to: "js/history.js"
      via: "import statement"
      pattern: "import.*from.*history.js"
    - from: "tests/history.test.js"
      to: "localStorage"
      via: "browser API"
      pattern: "localStorage\\.(getItem|setItem|clear)"
---

<objective>
Create browser-mode tests for history tracking and localStorage persistence using real browser APIs.

Purpose: Verify history management, localStorage persistence, quota handling, and FIFO eviction without mocking. Browser mode provides real localStorage behavior that jsdom cannot simulate reliably.

Output: tests/history.test.js with comprehensive localStorage integration tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-testing/06-RESEARCH.md
@.planning/phases/06-automated-testing/06-01-SUMMARY.md
@js/history.js

Key behaviors to test:
- addToHistory() stores calculation in memory and localStorage
- loadHistory() retrieves from localStorage
- clearHistory() removes all entries
- MAX_HISTORY_ENTRIES = 50 limit enforced (FIFO)
- Quota exceeded errors handled gracefully
- History excludes error results (division by zero)
- Only binary operations tracked (not unary operations)
</context>

<tasks>

<task type="auto">
  <name>Create history.test.js with browser-mode localStorage tests</name>
  <files>tests/history.test.js</files>
  <action>
    Create tests/history.test.js with browser environment directive at top:
    ```javascript
    // @vitest-environment browser
    import { describe, it, expect, beforeEach } from 'vitest'
    import { addToHistory, loadHistory, clearHistory, MAX_HISTORY_ENTRIES, HISTORY_KEY } from '../js/history.js'
    ```

    Create test suites:

    1. Basic persistence (describe block):
       - addToHistory('2 + 3', '5') persists to localStorage
       - loadHistory() retrieves from localStorage with correct structure
       - Verify localStorage.getItem(HISTORY_KEY) contains valid JSON

    2. Clear functionality:
       - clearHistory() removes all entries from memory and localStorage
       - loadHistory() returns empty array after clear

    3. Max entries enforcement:
       - Add 60 calculations (exceeds MAX_HISTORY_ENTRIES)
       - Verify history.length <= 50
       - Verify oldest entries removed (FIFO)

    4. Edge cases:
       - Empty history initial state
       - Single entry persistence
       - Rapid successive additions

    5. Error handling:
       - isStorageSupported() detection (may need to test separately)
       - Graceful degradation when localStorage unavailable

    Use beforeEach to call localStorage.clear() and clearHistory() to reset state.

    Reference pattern from 06-RESEARCH.md "Browser Mode Test for localStorage" section.

    IMPORTANT: These tests MUST use browser mode (@vitest-environment browser comment) to access real localStorage. Do NOT mock localStorage.
  </action>
  <verify>npm test shows browser tests passing with "browser mode" in output, coverage includes history.js</verify>
  <done>tests/history.test.js exists with passing browser-mode tests for localStorage</done>
</task>

</tasks>

<verification>
Run these checks:
1. npm test passes with browser-mode tests
2. Test output shows "browser" environment indicator
3. npm run test:coverage shows history.js coverage >80%
4. localStorage operations tested with real browser API
5. Tests handle async behavior correctly (if any)
</verification>

<success_criteria>
- history.test.js uses browser mode for real localStorage testing
- Persistence, retrieval, and clearing tested
- MAX_HISTORY_ENTRIES limit verified
- FIFO eviction tested with >50 entries
- All tests pass consistently
- Coverage >80% for history.js
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-testing/06-03-SUMMARY.md`
</output>
