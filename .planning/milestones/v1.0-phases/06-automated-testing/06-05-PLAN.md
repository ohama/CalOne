---
phase: 06-automated-testing
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - .github/workflows/test.yml
  - .gitignore
  - README.md
autonomous: true

must_haves:
  truths:
    - "GitHub Actions runs tests on every push"
    - "CI workflow installs Playwright and runs all tests"
    - "Test status visible in repository"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "CI workflow for automated testing"
      contains: "npm test"
    - path: ".gitignore"
      provides: "Excludes node_modules and coverage from git"
      contains: "node_modules/"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "npm test"
      via: "run command"
      pattern: "run: npm test"
---

<objective>
Set up GitHub Actions CI workflow for automated test execution on every push and pull request.

Purpose: Ensure all tests run automatically in CI without human interaction. Provides continuous validation that calculator code changes don't break existing functionality.

Output: Working CI workflow, updated .gitignore, and README with test instructions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-testing/06-RESEARCH.md
@.planning/phases/06-automated-testing/06-02-SUMMARY.md
@.planning/phases/06-automated-testing/06-03-SUMMARY.md
@.planning/phases/06-automated-testing/06-04-SUMMARY.md

CI workflow requirements:
- Run on push to main and all pull requests
- Node.js 20+ for modern JavaScript support
- Install dependencies with npm ci
- Install Playwright browsers for browser-mode tests
- Run tests and generate coverage report
- Fail workflow if any tests fail
</context>

<tasks>

<task type="auto">
  <name>Create GitHub Actions test workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Create .github/workflows/test.yml with:

    - name: "Automated Tests"
    - triggers: on.push.branches: [main], on.pull_request.branches: [main]
    - job: test on ubuntu-latest
    - steps:
      1. Checkout code (actions/checkout@v4)
      2. Setup Node.js 20 (actions/setup-node@v4 with cache: 'npm')
      3. Install dependencies (npm ci)
      4. Install Playwright browsers (npx playwright install --with-deps chromium)
      5. Run tests (npm test)
      6. Generate coverage (npm run test:coverage)
      7. Upload coverage artifact (actions/upload-artifact@v4 with path: ./coverage)

    Reference pattern from 06-RESEARCH.md "GitHub Actions CI Workflow" section.

    Use npm ci (not npm install) in CI for reproducible builds from package-lock.json.

    Only install chromium browser (not all browsers) to speed up CI.
  </action>
  <verify>.github/workflows/test.yml exists, contains npm test and playwright install steps</verify>
  <done>GitHub Actions workflow exists and will run tests on every push</done>
</task>

<task type="auto">
  <name>Update .gitignore and README</name>
  <files>.gitignore, README.md</files>
  <action>
    Create or update .gitignore to exclude:
    ```
    node_modules/
    coverage/
    .DS_Store
    *.log
    ```

    Update README.md to add Testing section:
    ```markdown
    ## Testing

    This project uses Vitest for automated testing.

    ### Running Tests

    \`\`\`bash
    # Install dependencies (first time only)
    npm install

    # Run all tests
    npm test

    # Run tests in watch mode
    npm run test:watch

    # Generate coverage report
    npm run test:coverage
    \`\`\`

    ### Test Structure

    - `tests/calculator.test.js` - Unit tests for calculation logic
    - `tests/history.test.js` - localStorage persistence tests (browser mode)
    - `tests/integration.test.js` - DOM interaction and integration tests (browser mode)

    Tests run automatically in GitHub Actions CI on every push.
    ```

    If README.md doesn't exist, create it with project title, description, and Testing section.
  </action>
  <verify>git check-ignore node_modules returns 0 (ignored), README.md contains Testing section</verify>
  <done>.gitignore excludes test artifacts, README documents how to run tests</done>
</task>

</tasks>

<verification>
Run these checks:
1. .github/workflows/test.yml is valid YAML
2. .gitignore excludes node_modules/ and coverage/
3. README.md has Testing section with npm commands
4. git status shows test.yml staged but node_modules/ not tracked
5. After pushing to GitHub, Actions tab shows workflow run
</verification>

<success_criteria>
- GitHub Actions workflow exists and runs on push/PR
- Workflow installs Playwright and runs all tests
- Coverage report generated in CI
- .gitignore excludes node_modules and coverage
- README documents test commands
- All tests pass in CI environment
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-testing/06-05-SUMMARY.md`
</output>
