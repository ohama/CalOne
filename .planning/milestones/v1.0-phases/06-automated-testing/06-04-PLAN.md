---
phase: 06-automated-testing
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/integration.test.js
autonomous: true

must_haves:
  truths:
    - "DOM button clicks update display correctly"
    - "Keyboard input triggers calculator functions"
    - "Full calculation flows work end-to-end"
  artifacts:
    - path: "tests/integration.test.js"
      provides: "Browser-mode integration tests for DOM interactions"
      min_lines: 100
      contains: "@vitest-environment browser"
  key_links:
    - from: "tests/integration.test.js"
      to: "js/main.js"
      via: "import statement"
      pattern: "import.*from.*main.js"
    - from: "tests/integration.test.js"
      to: "document.getElementById"
      via: "DOM API"
      pattern: "document\\.(getElementById|querySelector)"
---

<objective>
Create integration tests for DOM interactions, button clicks, and keyboard events using browser mode.

Purpose: Verify that calculator works end-to-end from user input (buttons/keyboard) to display output. Tests full user flows including multi-step calculations and history integration.

Output: tests/integration.test.js with comprehensive DOM integration tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-testing/06-RESEARCH.md
@.planning/phases/06-automated-testing/06-01-SUMMARY.md
@js/main.js
@index.html

Key behaviors to test:
- Button clicks trigger calculator operations
- Display updates reflect calculator state
- Keyboard events trigger same operations as buttons
- Event delegation handles all button types
- Full calculation flows: 2 + 3 = shows 5
- History panel updates when calculation completes
</context>

<tasks>

<task type="auto">
  <name>Create integration.test.js with DOM and event tests</name>
  <files>tests/integration.test.js</files>
  <action>
    Create tests/integration.test.js with browser environment directive:
    ```javascript
    // @vitest-environment browser
    import { describe, it, expect, beforeEach } from 'vitest'
    import { updateDisplay, handleButtonClick, handleKeyboardInput } from '../js/main.js'
    import { calculator, resetCalculator } from '../js/calculator.js'
    import { clearHistory } from '../js/history.js'
    ```

    Create test suites:

    1. DOM setup and display updates:
       - Create minimal DOM structure in beforeEach
       - updateDisplay() changes display element textContent
       - Display shows calculator.displayValue

    2. Button click handling:
       - Click digit button updates display
       - Click operator button changes state
       - Click equals shows result
       - Full flow: click 2, +, 3, = shows 5

    3. Keyboard event handling:
       - Dispatch KeyboardEvent for digits (key: '2')
       - Dispatch for operators (key: '+')
       - Dispatch Enter for equals
       - Dispatch Escape for clear
       - Dispatch Backspace for delete
       - Verify calculator functions called

    4. Event delegation:
       - Click on button element triggers action
       - Click on button child (span) still works via closest()
       - Click outside buttons doesn't trigger

    5. Integration with history:
       - Complete calculation adds to history
       - History panel updates (if rendered)
       - Clear history button works

    Use beforeEach to:
    - Set up minimal HTML structure (display, buttons)
    - Call resetCalculator() and clearHistory()
    - Clear localStorage

    Reference pattern from 06-RESEARCH.md "Integration Test with DOM" section.

    IMPORTANT: Create minimal HTML structure programmatically in tests using document.body.innerHTML. Don't load full index.html (too heavy).
  </action>
  <verify>npm test shows integration tests passing, browser mode used, coverage includes main.js</verify>
  <done>tests/integration.test.js exists with passing tests for button clicks, keyboard events, and full flows</done>
</task>

</tasks>

<verification>
Run these checks:
1. npm test passes all integration tests
2. DOM manipulation tests work in browser mode
3. Button click and keyboard event tests pass
4. Coverage report includes main.js (>60% acceptable for DOM-heavy code)
5. Full calculation flows tested end-to-end
</verification>

<success_criteria>
- integration.test.js uses browser mode for DOM testing
- Button clicks update display correctly
- Keyboard events trigger calculator operations
- Event delegation tested (clicks on various elements)
- Full calculation flows verified (2 + 3 = 5)
- Integration with history tested
- All tests pass consistently
- Coverage >60% for main.js
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-testing/06-04-SUMMARY.md`
</output>
