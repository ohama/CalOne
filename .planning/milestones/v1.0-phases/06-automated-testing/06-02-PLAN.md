---
phase: 06-automated-testing
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/calculator.test.js
autonomous: true

must_haves:
  truths:
    - "All calculator arithmetic operations are tested"
    - "Edge cases (division by zero, floating point precision) have passing tests"
    - "Tests run in seconds without human interaction"
  artifacts:
    - path: "tests/calculator.test.js"
      provides: "Unit tests for pure calculator functions"
      min_lines: 100
      contains: "describe('Calculator"
  key_links:
    - from: "tests/calculator.test.js"
      to: "js/calculator.js"
      via: "import statement"
      pattern: "import.*from.*calculator.js"
---

<objective>
Create comprehensive unit tests for calculator pure functions covering all arithmetic operations and edge cases.

Purpose: Verify calculation accuracy, floating-point precision handling, and error states through automated tests. These tests run in Node environment (fast) since they don't need browser APIs.

Output: tests/calculator.test.js with 100% coverage of calculator arithmetic logic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-testing/06-RESEARCH.md
@.planning/phases/06-automated-testing/06-01-SUMMARY.md
@js/calculator.js

Key behaviors to test:
- Basic arithmetic: 2 + 3 = 5, 10 - 4 = 6, 5 × 3 = 15, 8 ÷ 2 = 4
- Floating-point precision: 0.1 + 0.2 = 0.3 (not 0.30000000000000004)
- Division by zero: 5 ÷ 0 = 'Error'
- Decimal input: prevents multiple decimal points
- Sign toggle: +5 becomes -5
- Percent calculation: contextual based on operator
- Backspace: handles edge cases (Error state, single digit, after operator)
- Operation chaining: 2 + 3 + 4 = 9
</context>

<tasks>

<task type="auto">
  <name>Create calculator.test.js with comprehensive unit tests</name>
  <files>tests/calculator.test.js</files>
  <action>
    Create tests/ directory and tests/calculator.test.js.

    Import test functions and calculator module:
    ```javascript
    import { describe, it, expect, beforeEach } from 'vitest'
    import { calculator, inputDigit, inputDecimal, handleOperator, resetCalculator, toggleSign, handlePercent, handleBackspace } from '../js/calculator.js'
    ```

    Create test suites:

    1. Basic arithmetic operations (describe block):
       - Addition: 2 + 3 = 5
       - Subtraction: 10 - 4 = 6
       - Multiplication: 5 × 3 = 15
       - Division: 8 ÷ 2 = 4

    2. Floating-point precision:
       - 0.1 + 0.2 = 0.3 (verify toFixed precision fix)
       - 0.3 - 0.1 = 0.2

    3. Edge cases:
       - Division by zero returns 'Error'
       - Decimal input prevents multiple decimals
       - Single digit backspace resets to '0'
       - Backspace on Error state resets

    4. Operator chaining:
       - 2 + 3 + 4 = 9
       - 10 - 2 - 3 = 5

    5. Unary operations:
       - Sign toggle: +5 becomes -5, -5 becomes +5
       - Percent: 200 + 10% = 220 (contextual)

    Use beforeEach to call resetCalculator() before each test.

    Test through public API (calculator.displayValue) not internal state.

    Reference pattern from 06-RESEARCH.md "Unit Test for Pure Calculator Functions" section.
  </action>
  <verify>npm test shows all tests passing, npm run test:coverage shows >90% coverage for calculator.js</verify>
  <done>tests/calculator.test.js exists with passing tests covering all calculator operations</done>
</task>

</tasks>

<verification>
Run these checks:
1. npm test passes with 0 failures
2. npm run test:coverage shows calculator.js coverage >90%
3. Tests complete in under 5 seconds
4. All arithmetic operations, edge cases, and unary operations covered
</verification>

<success_criteria>
- calculator.test.js tests all arithmetic operations (+, -, ×, ÷)
- Edge cases tested: division by zero, floating-point precision, decimal handling
- Unary operations tested: sign toggle, percent
- Backspace edge cases covered
- All tests pass consistently
- Coverage >90% for calculator.js
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-testing/06-02-SUMMARY.md`
</output>
